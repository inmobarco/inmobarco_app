// Smoke test for InmobarcoApp is skipped because it requires dotenv
// to be loaded (assets/config/.env), which is not available in unit test
// environment. Integration tests should cover full app startup.
//
// The real test coverage comes from model and provider tests.

void main() {
  // Placeholder — see test/domain/ and test/ui/ for real tests.
}
