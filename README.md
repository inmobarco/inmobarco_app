# inmobarco_app

A new Flutter project.

## TODO

- [ ] Mover `getZonesByCity` de `WasiApiService` a `GlobalDataService` (o crear un provider dedicado) para evitar instanciar `WasiApiService` directamente en screens (`add_property_screen`, `property_filter_screen`)
